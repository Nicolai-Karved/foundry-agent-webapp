# Auth/Session security (repo-verifiable)

- Password policy configuration present in code or config (length, complexity, uniqueness).
- Session cookie/token settings configured (Secure, HttpOnly, SameSite, rotation/expiry) in code or config.
- Authentication and authorization rules implemented and enforced in code.
- Use OWASP ASVS or relevant OWASP project guidance when governance is silent.
